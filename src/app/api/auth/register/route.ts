import { NextResponse } from "next/server";
import { createSession, register } from "@/lib/auth";
import { issueCsrfToken, rateLimit } from "@/lib/request-security";
export const runtime = "nodejs";
export async function POST(request: Request) { const retryAfter = rateLimit(request, "register", 5); if (retryAfter) return NextResponse.json({ message: "Muitas tentativas. Aguarde antes de tentar novamente." }, { status: 429, headers: { "Retry-After": String(retryAfter) } }); try { const input = await request.json(); const user = register(input); const response = NextResponse.json({ message: "Conta criada", next: "/dashboard", user: { id: user.id, name: user.name, role: user.role, storeName: user.storeName } }, { status: 201 }); response.cookies.set("vendio_session", createSession(user), { httpOnly: true, secure: process.env.NODE_ENV === "production", sameSite: "lax", priority: "high", path: "/", maxAge: 60 * 60 * 8 }); response.cookies.set("vendio_csrf", issueCsrfToken(), { secure: process.env.NODE_ENV === "production", sameSite: "strict", priority: "high", path: "/", maxAge: 60 * 60 * 8 }); return response; } catch (error) { return NextResponse.json({ message: error instanceof Error ? error.message : "Dados inválidos." }, { status: 400 }); } }
