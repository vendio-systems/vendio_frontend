import { moveStock, recentMovements } from "@/lib/erp";
import { requireRole, verifyCsrf } from "@/lib/request-security";
export const runtime = "nodejs";
export async function GET(request: Request) { const access = requireRole(request, ["CLIENT", "ADMIN", "DEVELOPER", "OWNER"]); return "error" in access || !access.session.storeId ? Response.json({ message: "error" in access ? access.error : "Loja não encontrada." }, { status: "error" in access ? access.status : 404 }) : Response.json({ data: recentMovements(access.session.storeId) }); }
export async function POST(request: Request) { const access = requireRole(request, ["CLIENT", "ADMIN", "DEVELOPER", "OWNER"]); if ("error" in access || !access.session.storeId) return Response.json({ message: "error" in access ? access.error : "Loja não encontrada." }, { status: "error" in access ? access.status : 404 }); if (!verifyCsrf(request)) return Response.json({ message: "Token CSRF inválido." }, { status: 403 }); try { const body = await request.json(); const result = moveStock(access.session.storeId, access.session.sub, { productId: body.productId, type: body.type, quantity: Number(body.quantity), location: String(body.location ?? ""), reason: String(body.reason ?? "") }); return Response.json({ data: result }, { status: 201 }); } catch (error) { return Response.json({ message: error instanceof Error ? error.message : "Movimento inválido." }, { status: 400 }); } }
